export enum ReplyProperties {
  MessengerSender,
}

export enum OperationType {
  Create = 'Create',
  Edit = 'Edit',
  Delete = 'Delete',
}

export enum EntityType {
  Post = 'Post',
  Reply = 'Reply',
  Comment = 'Comment',
}
